server:
  port: 8090

spring:
  application:
    name: BouAliJWTSecurity

  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_security
    username: postgres
    password: admin@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
#    database-platform: org.hibernate.dialect.PostgreSQLDilect